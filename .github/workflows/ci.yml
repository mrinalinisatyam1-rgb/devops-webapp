name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Display Files
        run: ls -la

      - name: Simulate Build
        run: echo "Build Successful!"

      - name: Slack Notification
        if: always()
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"GitHub CI Pipeline Completed Successfully!"}' \
          ${{ secrets.SLACK_WEBHOOK_URL }}